import {StyleSheet} from 'react-native';
import {mediumSmall} from 'shared/theme/fontsize';

export default StyleSheet.create({
  viewContainer: {
    flex: 1,
    alignItems: 'center',
    backgroundColor: 'lightgrey',
    // opacity: 0.5,
  },
  horizontal: {
    // flexDirection: 'row',
    // justifyContent: 'space-around',
    padding: 10,
  },
});
